const { Interaction } = require("discord.js");
const ParticipantManager = require("./Participant");

const leaderboard = {
  /**
   * 
   * @param {Interaction} interaction 
   * @param {ParticipantManager} participants 
   */

  async command(interaction, participants) {
    //TODO: Implement this function. See the Module.Holiday.PristineWaters.js file for an example for some of the functionality desired.
    //E.g. Embeds, Pagination, Sorting by multiple values etc.
    //TODO: for @chevyboys, integrate this file with the controller

  }
}

module.exports = leaderboard;