const Inventory = require('./Inventory.Class.js');

module.exports = {
  /**
   * 
   * @param {Interaction} interaction 
   * @param {ParticipantManager} participants 
   */
  inventoryCommand: async (interaction, participants) => {
    //TODO: Implement this function @SapphireSapphic
  }
}